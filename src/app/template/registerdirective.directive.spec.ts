import { RegisterdirectiveDirective } from './registerdirective.directive';

describe('RegisterdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
